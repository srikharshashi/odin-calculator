const calctextdiv = document.querySelector(".cont1");
const calcobj={
    operand1:0,
    operator:0,
    operand2:0,
    result: undefined,
};
calctextdiv.textContent=
const buttons = document.querySelectorAll(".buttons");
buttons.forEach((button) => {
    button.addEventListener("click", (event) => {});
});